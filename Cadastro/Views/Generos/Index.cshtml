@model IEnumerable<Cadastro.Models.Genero>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.faixaEtaria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.faixaEtaria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codGenero }) |
            @Html.ActionLink("Details", "Details", new { id=item.codGenero }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codGenero })
        </td>
    </tr>
}

</table>
